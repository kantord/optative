[package]
name = "esto"
version = "0.0.11"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "A CLI (esto) for running declarative .op.tsx/.eso.jsx reconciler scripts."
keywords = ["reconciler", "desired-state", "lifecycle", "cli", "jsx"]
categories = ["command-line-utilities", "os::unix-apis"]

[[bin]]
name = "esto"
path = "src/main.rs"

[dependencies]
optative-script = { version = "0.0.12", path = "../optative-script" }
optative-script-mdx = { version = "0.0.6", path = "../optative-script-mdx" }
thiserror = "2"
tracing-subscriber = "0.3"
clap = { version = "4", features = ["derive"] }
rquickjs = { version = "0.12", features = ["loader"] }
notify = "8"
glob = "0.3"

[build-dependencies]
ts-rs = "12"

[dev-dependencies]
tempfile = "3"
